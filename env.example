# 🌐 API 요청 패턴: 무조건 환경변수 사용 (하드코딩 금지!)

# 로컬 개발용 (.env.local에 복사해서 사용)
# /api → Vite dev server proxy → http://52.79.237.86:8080
VITE_API_BASE_URL=/api
VITE_USE_MSW=false

# Vercel 환경변수로 설정:
# Preview: VITE_API_BASE_URL=https://52.79.237.86:8080 (직접 연결)
# Production: VITE_API_BASE_URL=https://52.79.237.86:8080 (직접 연결)

# 최종 요청 URL 예시:
# 로컬: /api/members/login → http://52.79.237.86:8080/members/login
# Vercel: https://52.79.237.86:8080/members/login (직접 연결)
