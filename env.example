# 🌐 API 요청 패턴: 무조건 환경변수 사용 (하드코딩 금지!)

# 로컬 개발용 (.env.local에 복사해서 사용)
# /api → Vite dev server proxy → http://52.79.237.86:8080
VITE_API_BASE_URL=/api
VITE_USE_MSW=false

# Vercel 환경변수로 설정:
# Preview: VITE_API_BASE_URL=/api (서버리스 함수 프록시 사용)
# Production: VITE_API_BASE_URL=/api (서버리스 함수 프록시 사용)
